<script setup lang="ts">import {ArrowUpRight,MessageCircle} from 'lucide-vue-next';import type { Product } from '~/types/product';defineProps<{product:Product}>();const ui=useUiStore();const {formatIDR}=useCurrency()</script>
<template><article class="card"><NuxtLink :to="`/produk/${product.slug}`" class="image"><NuxtImg :src="product.coverImage.url" :alt="product.coverImage.alt" width="640" height="480" loading="lazy" format="webp"/></NuxtLink><div class="body"><div class="meta"><span>{{product.category.name}}</span><small>{{product.sku}}</small></div><NuxtLink :to="`/produk/${product.slug}`" class="title">{{product.name}}</NuxtLink><strong>{{formatIDR(product.price)}}</strong><div class="actions"><ProductStockBadge :stock="product.stock" :threshold="product.lowStockThreshold"/><NuxtLink :to="`/produk/${product.slug}`" aria-label="Lihat detail"><ArrowUpRight :size="17"/></NuxtLink></div><button class="btn btn-outline" @click="ui.orderProduct=product"><MessageCircle :size="16"/>{{product.stock?'Pesan via WhatsApp':'Tanya ketersediaan'}}</button></div></article></template>
<style scoped>.card{border:1px solid var(--border);border-radius:16px;overflow:hidden;background:white}.card:hover{border-color:var(--gold-soft)}.image{display:block;aspect-ratio:4/3;overflow:hidden;background:var(--surface-2)}.image img{width:100%;height:100%;object-fit:cover;transition:transform .2s}.card:hover .image img{transform:scale(1.02)}.body{padding:1rem;display:grid;gap:.75rem}.meta{display:flex;justify-content:space-between;color:var(--gold-dark);font-size:.68rem;font-weight:800;text-transform:uppercase;letter-spacing:.08em}.meta small{color:var(--muted)}.title{font-weight:750;line-height:1.35;min-height:2.7em}.actions{display:flex;align-items:center;justify-content:space-between}.body>.btn{width:100%;font-size:.8rem}</style>
