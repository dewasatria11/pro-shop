<script setup lang="ts">import { stockStatus } from '~~/shared/utils/format';const p=defineProps<{stock:number;threshold:number}>();const label=computed(()=>stockStatus(p.stock,p.threshold))</script><template><span class="badge" :class="label.toLowerCase()">{{label==='Tersedia'?`Stok ${stock}`:label==='Menipis'?`Stok menipis · ${stock}`:'Stok habis'}}</span></template><style scoped>.tersedia{color:var(--success);background:#e9f5ef}.menipis{color:var(--warning);background:#fbf0df}.habis{color:var(--danger);background:#faeaea}</style>
