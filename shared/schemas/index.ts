import { z } from 'zod'
export const slugSchema=z.string().trim().min(2).max(140).regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/,'Slug hanya boleh berisi huruf kecil, angka, dan tanda hubung.')
export const productSchema=z.object({name:z.string().trim().min(2).max(120),sku:z.string().trim().min(2).max(50),slug:slugSchema,categoryId:z.string().min(1),price:z.number().int().min(0),stock:z.number().int().min(0).optional(),lowStockThreshold:z.number().int().min(0),shortDescription:z.string().max(220).default(''),description:z.string().max(5000).default(''),specifications:z.record(z.string(),z.string()).default({}),images:z.array(z.object({url:z.url(),alt:z.string().max(160),storageKey:z.string().optional(),isCover:z.boolean().optional(),sortOrder:z.number().int().optional()})).max(6).default([]),isActive:z.boolean(),isFeatured:z.boolean()})
export const categorySchema=z.object({name:z.string().trim().min(2).max(80),slug:slugSchema,description:z.string().max(300).default(''),sortOrder:z.number().int().min(0).default(0),isActive:z.boolean().default(true)})
export const movementSchema=z.object({productId:z.string().min(1),quantity:z.number().int().positive(),movementAt:z.iso.datetime().optional(),reference:z.string().max(100).optional(),counterparty:z.string().max(120).optional(),note:z.string().max(500).optional()})
export const loginSchema=z.object({identifier:z.string().trim().min(3).max(120),password:z.string().min(8).max(128)})
