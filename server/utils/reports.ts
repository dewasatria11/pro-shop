import type { H3Event } from 'h3';export function reportFilter(event:H3Event){const q=getQuery(event),c:string[]=['1=1'],p:unknown[]=[];if(q.from){c.push('date(m.movement_at)>=date(?)');p.push(q.from)}if(q.to){c.push('date(m.movement_at)<=date(?)');p.push(q.to)}if(q.product){c.push('m.product_id=?');p.push(q.product)}if(q.category){c.push('c.slug=?');p.push(q.category)}if(q.type){c.push('m.movement_type=?');p.push(q.type)}if(q.keyword){c.push('(m.reference LIKE ? OR m.note LIKE ?)');p.push(`%${q.keyword}%`,`%${q.keyword}%`)}return {where:c.join(' AND '),params:p}}
